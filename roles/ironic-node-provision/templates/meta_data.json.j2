{
    "availability_zone": "nova",
    "files": [ ],
    "hostname": "{{ hostname | default(node_name) }}",
    "launch_index": 0,
    "name": "{{ hostname | default(node_name) }}",
    "meta": {
        "role": "webservers",
        "essential": "false"
    },
    "public_keys": {
        "mykey": "{{ ssh_public_key }}"
    },
    "uuid": "{{ node_uuid.stdout }}"
}

